# promptuna-cli

Typer CLI for on-disk promptuna projects. Installed as a separate package (pulls in `promptuna`):

```bash
pip install promptuna-cli
```

Overview, project layout, and job output semantics: see [src/promptuna_cli/SKILL.md](src/promptuna_cli/SKILL.md) (also shown by `promptuna --help`).
