# promptuna-cli (planned)

Future uv workspace member: a thin Typer CLI that runs `run`, `evaluate`, and `optimize` against on-disk projects via `promptuna.projects`, with a `SKILL.md` template for coding-agent workflows. Install with `pip install promptuna[cli]` once it ships.
