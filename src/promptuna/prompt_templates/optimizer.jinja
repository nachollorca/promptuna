We have a Program: a function that calls a language model to accomplish a task.
Its output quality is measured by one or more metrics over a fixed dataset.
Each metric is normalized to [0, 1] and combined into a single headline score (higher is better).

**We are searching for a prompt template that maximizes that headline score.**

# Program

The **program** is everything that turns inputs into evaluated outputs. It has two parts:

1. **Scaffold** — deterministic code before and after the LM call (argument shaping, truncation, template rendering, parsing, coercion, fallbacks, etc.). Not editable.
2. **Completion** — the single LM call: **prompt template** (the only part you may edit), **output schema** (fixed structured-output contract, when present), and model.

We are searching exclusively over the prompt template space.
Consider the scaffold, output schema, and model fixed.

{% if OUTPUT_SCHEMA %}
## Output schema

The program passes a Pydantic ``BaseModel`` (possibly nested) to ``complete(..., output_schema=...)``.
The JSON Schema below describes that model — the structured-output contract enforced on the model.
Do not restate constraints it already guarantees (types, enums, required fields);
spend the template on *populating* these fields well.

```json
{{ OUTPUT_SCHEMA }}
```
{% endif %}

## Program implementation (read-only)

Full Python source of the program under evaluation: scaffold, completion call, and output schema.
Account for the scaffold — truncation, parsing, coercion, fallbacks — instead of duplicating or fighting it from the template.

```python
{{ PROGRAM_SOURCE }}
```

# Metrics

The headline score is the mean across these metrics (each weighted equally).
Metrics are not editable — use each description as the rubric for what “good” means.

{% if METRICS %}
{{ METRICS }}
{% endif %}

# Trajectory

These are the templates attempted at each step, with the results they produced, in chroncological order.
The headline score is the optimization objective: the mean across per-metric means (each metric weighted equally).

Each quality table cell is `mean ± sd (n)`.
`Score` aggregates per-example means (dispersion = dataset heterogeneity).
`Replicate noise` is the average within-cell SD across replicates (dispersion = measurement instability).

Trial failures count against the run (the program is under evaluation); scorer failures are excluded from quality aggregates.

Each `## Step N` heading shows role (baseline or candidate), headline score, delta vs baseline, and `⭐ best` on the winning step so far.

Each proposed step (not the baseline) opens with an **Intent** block — the hypothesis and edit plan that motivated the template change — then the verbatim template in a `template` fenced block, then the quality and reliability results it produced.

Error analysis appears only for the best (⭐) and latest checkpoints.
When present, it surfaces the weakest examples, with the rendered prompt the LM saw (in a `rendered_prompt` fenced block), the program output, and its per-metric scores.

{{ HISTORY }}

{% if PRIOR_RATIONALE %}
# Prior proposal rationale

This section contains the structured analysis produced **before** the latest checkpoint was evaluated.
It accounts for the thinking process accounting for the whole trajectory up to this point.
Use it to calibrate intent vs outcome; do not treat it as instructions you must follow.

{{ PRIOR_RATIONALE }}
{% endif %}

# Goal

Study the trajectory: infer what changes helped, what hurt, and where the current best prompt still fails.
Metric descriptions state the rubric; scores and judge reasons on prior steps reveal what the template still leaves implicit.
Infer that missing rubric and encode it in the new template so the model is steered toward higher scores.
Then write an improved prompt template.
You may refine the best checkpoint or explore a completely different approach.
You may add, edit or remove instructions to study their effect, or rewrite the template entirely from scratch.

Keep every Jinja placeholder (enclosed in double curly braces) from the templates above exactly as-is (same names and syntax).
Removing or renaming them breaks rendering and makes the template unusable.

If the real bottleneck lies outside the template, you can record it additionally as an advice for human review.
These can be:
- Changes in the output schema fields or their descriptions
- Changes to the scaffold (e.g. truncation before the call, coercion after)
- The model seems uncapable to adhere to the instructions
- The example data is flawed, contradictory or plainly wrong
You must still explore different options in any of these scenarios.
